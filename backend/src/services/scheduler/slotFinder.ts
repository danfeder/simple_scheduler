import { Class, Period } from 'shared/types';

export class SlotFinder {
  findAvailableSlots(classes: Class[], period: Period): Date[] {
    // Implementation will be added later
    return [];
  }
} 